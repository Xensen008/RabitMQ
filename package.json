{
  "name": "rabbitmq-examples",
  "version": "1.0.0",
  "description": "RabbitMQ examples in JavaScript",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "amqplib": "^0.10.3"
  }
}
